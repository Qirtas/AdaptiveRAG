# RAGForDatamite — Adaptive Retriever Test Pipeline

The main.py runs the full RAG pipeline to test adaptive retrieval.  
It can index CSV files into a Chroma vector DB and then test the Adaptive Retriever + LLM response generation (Claude).
